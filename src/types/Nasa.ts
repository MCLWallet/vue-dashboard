type Nasa = {
  copyright: string;
  date: string; // Format: YYYY-MM-DD
  explanation: string;
  hdurl: string;
  media_type: "image" | "video"; // Assuming media_type could be either "image" or "video"
  service_version: string;
  title: string;
  url: string;
};
